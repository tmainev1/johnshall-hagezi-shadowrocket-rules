name: build-merged-list

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"
          cache: true   # 需要 go.sum 存在才会缓存成功

      - name: Go mod tidy (generate go.sum)
        run: |
          go mod tidy
          test -f go.sum && echo "go.sum present"

      - name: Build & Run
        run: |
          go run ./cmd/merge

      - name: Commit changes (outputs + cache)
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add output/*.txt output/*.conf cache/ok.txt || true
            git commit -m "chore: auto-build $(date -u +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "No changes."
          fi

      - name: Make TS (yyyymmddhhmm)
        id: ts
        run: echo "ts=$(date -u +'%Y%m%d%H%M')" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ steps.ts.outputs.ts }}
          tag_name: ${{ steps.ts.outputs.ts }}
          files: |
            output/domains.txt
            output/shadowrocket.conf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
